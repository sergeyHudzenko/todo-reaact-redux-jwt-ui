import SingModule from "./SingModule";

const Header = () => {
  return (
    <header>
        <section className="title-section">
            <SingModule />
        </section>
    </header>
  );
};

export default Header;
